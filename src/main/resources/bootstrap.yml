spring:
  application:
    name: collect
  jackson:
    default-property-inclusion: non_null
server:
  port: 8586

logging:
  level:
    root: ERROR
    org.springframework.web: ERROR
    org.hibernate: DEBUG
    com.sscf: DEBUG